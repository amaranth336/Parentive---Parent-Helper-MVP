/**
 * Supabase Module
 * 
 * Centralized exports for Supabase client and types.
 */

export { createBrowserClient, createServerClient } from './client';
export type { Database, SupportRequest, SupportRequestStatus } from './types';
